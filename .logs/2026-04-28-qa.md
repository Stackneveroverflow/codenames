# QA

## Inventory

- 单元与集成验证：
  - `packages/game-core/test/game-core.test.ts`
  - `apps/server/test/deckService.test.ts`
  - `apps/server/test/roomStore.test.ts`
- 类型与构建验证：
  - `corepack pnpm -r lint`
  - `corepack pnpm -r build`
- 手工 UI/Playwright：
  - 本轮未接入 Playwright 脚本，也未产出截图文件。

## Results

- 通过：
  - 经典牌面分布、额外起手牌、刺客结束、胜负判定。
  - 出线索后猜测次数计算、猜错切换回合、主动结束回合。
  - AI 词牌校验、解析成功路径、无 OpenAI client 时回退、本地 fallback 牌组可用。
  - 建房、入房、开始对局、断线重连、房主移交、越权操作拒绝。
  - 全 workspace `lint` 通过。
  - 全 workspace `test` 通过。
  - 全 workspace `build` 通过。

## Commands

- `corepack pnpm install`
- `corepack pnpm -r lint`
- `corepack pnpm -r test`
- `corepack pnpm -r build`

## Gaps

- 未完成 Playwright Interactive QA。
- 未验证真实 OpenAI 联网发牌，仅验证了结构化解析接口与 fallback 兜底路径。
- 未做浏览器多实例联机手工演练；当前信心主要来自 store 测试和构建通过。
