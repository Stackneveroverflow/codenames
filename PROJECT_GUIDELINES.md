# 项目体验与代码准则

## 视觉规范

- 必须引入 Tailwind CSS，并优先使用 Tailwind 工具类或基于 Tailwind 的全局样式组织视觉层。
- 统一使用卡片布局、`rounded-xl` 圆角、`shadow` 阴影和渐变色背景。
- 响应式适配手机和电脑，页面保持居中布局，并提供充足留白。
- 配色采用复古风，优先使用纸张、黄铜、酒红、墨绿、暗蓝等低饱和色。

## 交互规范

- 所有按钮和可点击元素都需要 hover 放大、active 缩小和颜色过渡。
- 全局交互动画默认使用 `0.2s` 平滑过渡。
- 支持电脑键盘和手机触摸双端操作，点击区域需要足够大。
- 关键操作需要防抖，防止误触和重复提交。
- 得分、胜利和失败状态需要提供可视化动画反馈。

## 游戏规范

- 增加轻量点击、发牌或得分音效，音效不能阻塞游戏流程。
- 清晰展示分数、房间状态、玩家状态和当前操作提示。
- 提供零门槛新手引导，帮助新玩家快速理解创建、加入和查看答案流程。

## 代码规范

- HTML/CSS/JS 职责分离，结构清晰，UI、状态和通信逻辑保持可维护边界。
- 添加简洁注释，只解释不直观或容易误改的逻辑。
- 禁止冗余代码，避免不必要的重渲染和重计算，保证游戏流畅不卡顿。
